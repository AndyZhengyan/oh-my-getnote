---
id: "99e2c754bffba1f6e706ccd6aad73a7d"
title: "OpenClaw记忆系统深度解析：从架构原理到实战优化"
type: "AI链接笔记"
tags: ["AI链接笔记", "OpenClaw", "AI记忆系统", "向量搜索"]
domain: "AI 核心技术与模型"
date: "2026-03-19"
connections:
  - noteId: "0da2c5a35a9139c251c895e175dfec46"
    score: 0.8659428247965514
    type: "semantic"
  - noteId: "86195e38c15793174b58f8a9a85585ae"
    score: 0.8632534246725204
    type: "semantic"
  - noteId: "e15992c149286f0f6d02aeb9c2b1356a"
    score: 0.8346851067325249
    type: "semantic"
  - noteId: "dea27dd1b60262f3ef004c82c1d829d1"
    score: 0.8278856040927199
    type: "semantic"
  - noteId: "bef5cb135e04a8249144cd3fb707a642"
    score: 0.811846507324712
    type: "semantic"
  - noteId: "657a049a41b54c2d82bf5d56fbceec82"
    score: 0.8102313058708498
    type: "semantic"
  - noteId: "7442e8c3b386fa95a8a2cc6d0c93e74e"
    score: 0.7996105685422029
    type: "semantic"
  - noteId: "a859eb67f8bd310c3ad8362b87d7c738"
    score: 0.7981083028795843
    type: "semantic"
x: 0.21418454640214457
y: -0.03206981037919909
---

原文： 【干货】小龙虾用户必看-如何让你的Openclaw过目不忘 🧠 记忆系统核心问题与架构概览 用户痛点 ：多名用户反馈OpenClaw使用一段时间后&rdquo;忘记&rdquo;历史对话内容，核心疑问在于&rdquo;为何宣传的记忆功能未达预期&rdquo;。 记忆架构核心要点 ： - 会话历史 与 向量搜索 为独立机制，分别服务于短期与长期记忆 - 6层上下文防护机制 管理有限窗口，从内存裁剪到LLM摘要压缩层层递进 - Compaction压缩 虽延长对话，但每次压缩导致约 30%信息损失 - 配置本地Embedding模型后检索效果显著：精确关键词查询得分0.74， 语义理解查询得分高达0.85 🏗️ 记忆系统全景架构 OpenClaw记忆系统由5个核心组件协同工作： 核心组件解析 会话历史（Session Transcript） 当前对话原始记录，存储为JSONL文件，直接注入LLM上下文 范围仅限当前session，无需embedding或检索 Memory Search（向量搜索） 跨会话长期记忆检索系统，依赖embedding模型 Memory文件切分、向量化后存入SQLite数据库，LLM需主动调用 memory_search 工具 Session-memory hook 连接短期与长期记忆的桥梁，执行 /new 时自动保存会话摘要至 memory/*.md Compaction压缩 上下文窗口不足时，LLM生成结构化摘要替代旧消息，伴随信息损失 Hybrid Search（混合检索） 向量相似度搜索（语义理解）与BM25关键词搜索（精确匹配）按70:30加权合并 协作流程 对话进行中 │ ├── 会话历史（当前 session 消息）──→ 直接注入 LLM 上下文 │ ├── 上下文快满了？──→ Context Pruning（裁剪旧 tool 结果） │ ──→ Memory Flush（关键信息写入 memory 文件） │ ──→ Compaction（LLM 生成摘要替代旧消息） │ ├── 用户执行 /new ──→ session-memory hook 保存摘要到 memory/*.md │ ──→ 文件被 embedding 索引到 SQLite │ └── 新 session 需要回忆？──→ LLM 调用 memory_search ──→ Hybrid Search（向量 + BM25） ──→ 返回相关记忆片段 🔍 会话历史 vs Memory Search深度对比 1.1 会话历史（Session Transcript） 存储方式 ：JSONL文件（ /agents/<agent>/sessions/<sessionId>.jsonl ） 数据结构 ：包含 type: "session" （头信息）、 type: "message" （对话内容）、 type: "compaction" （压缩记录） 工作流程 ：加载→验证→截断→直接注入LLM prompt，无需API调用 核心特点 ：100%精确但受context window限制 1.2 Memory Search（向量搜索） 存储方式 ：SQLite数据库（ ~/.openclaw/memory/<agent>.sqlite ） 工作流程 ：文件监听→切分chunks→embedding→存入向量表+全文索引→LLM主动调用工具检索 触发机制 ：系统提示词要求LLM在涉及&rdquo;prior work, decisions, dates, people, preferences, or todos&rdquo;时必须调用 1.3 两者的桥梁机制 session-memory hook ： /new 命令触发，保存最近15条消息摘要至 memory/YYYY-MM-DD-<slug>.md memory flush ：上下文接近极限时（ totalTokens >= contextWindow - 24,000 ），LLM将关键信息写入memory文件 1.4 核心区别对照 维度 会话历史 Memory Search 范围 当前 session 内 跨所有 session 存储 JSONL 文件（消息数组） SQLite 向量数据库 需要 embedding 不需要 需要 容量 受 context window 限制 理论上无限（检索 top-k） 精确度 100%（原文在上下文里） 依赖检索质量 发送方式 自动作为 conversation history LLM 主动调用 tool 持久性 session 结束可能丢失 文件删除前永久存在 💾 Memory文件与数据库结构 数据库现状 ： - 单文件 main.sqlite （15MB，3,729 pages） - 无 MEMORY.md 文件，session-memory hook已启用 - 包含5张核心表： meta （元数据）、 files （追踪文件）、 chunks （文本块及向量）、 embedding_cache （API缓存）、 chunks_fts （FTS5索引） 🔌 Embedding机制与故障处理 3.1 Embedding Provider选择链 自动模式优先级： local (node-llama-cpp) → OpenAI → Gemini → Voyage - 本地模型文件存在（如 embeddinggemma-300M-Q8_0.gguf ）则优先使用 - 环境变量检测： OPENAI_API_KEY → GEMINI_API_KEY → VOYAGE_API_KEY - 无可用provider时，memory search自动禁用 3.2 搜索流程缺陷 src/memory/manager.ts 中 search() 方法存在设计缺陷： search() 流程: 1. BM25 关键词搜索 (结果已计算) 2. embedQueryWithTimeout() (若抛异常...) 3. 整个 search() 抛出错误，BM25 结果丢失! 改进建议 ：为 embedQueryWithTimeout() 添加try/catch，embedding失败时退化为纯BM25搜索 3.3 各阶段失败影响 阶段 失败处理 索引/同步 有 fallback — 切换 provider 重试 搜索查询 无 fallback — embedding 失败 = 整个搜索失败 工具层 memory-tool.ts 有 try/catch，返回 { results: [], disabled: true } Agent 层 LLM 看到 search disabled，继续不带记忆地回答 📝 JSONL会话文件管理机制 4.1 上下文管理的6层防护 防护层级 触发条件 具体动作 1. Context Pruning 30% context占用 Soft-trim ：旧tool结果截断到4,000字符（头1,500+尾1,500） 50% context占用 Hard-clear ：旧tool结果替换为&rdquo;[Old tool result content cleared]&rdquo; 2. Memory Flush totalTokens >= contextWindow - 24,000 LLM将重要信息写入 memory/YYYY-MM-DD.md 3. Compaction 上下文接近极限 LLM生成结构化摘要替代旧消息，写入 type: "compaction" 记录 4. Overflow自动重试 API返回context overflow 最多3次自动压缩重试，失败后提示用户 /reset 或 /new 5. History Turn Limit 配置 dmHistoryLimit 默认不限制，群聊默认50条 6. Tool Result截断 单tool结果过大 最多占30% context window，硬上限400,000字符 4.2 关键常量汇总 常量 值 文件 DEFAULT_CONTEXT_TOKENS 200,000 src/agents/defaults.ts:6 CONTEXT_WINDOW_HARD_MIN_TOKENS 16,000 src/agents/context-window-guard.ts:3 DEFAULT_PI_COMPACTION_RESERVE_TOKENS_FLOOR 20,000 src/agents/pi-settings.ts:3 MAX_OVERFLOW_COMPACTION_ATTEMPTS 3 src/agents/pi-embedded-runner/run.ts:386 MAX_TOOL_RESULT_CONTEXT_SHARE 0.3 (30%) src/agents/pi-embedded-runner/tool-result-truncation.ts:11 HARD_MAX_TOOL_RESULT_CHARS 400,000 src/agents/pi-embedded-runner/tool-result-truncation.ts:19 DEFAULT_GROUP_HISTORY_LIMIT 50 src/auto-reply/reply/history.ts:4 📦 Compaction压缩机制详解 5.1 JSONL树形结构 JSONL文件通过 id 和 parentId 形成追加式树结构： session头 → msg1 → msg2 → ... → msg50 → compaction1 → msg51 → ... ↑ firstKeptEntryId = msg45 加载时 buildSessionContext() 回溯路径，遇到compaction记录则跳过 firstKeptEntryId 前的消息 5.2 压缩后LLM看到的内容 [CompactionSummary(user角色), msg45, msg46, ..., msg50, msg51, ...] ↑ 摘要替代了 msg1-msg44 ↑ kept messages ↑ 新消息 摘要以user角色消息呈现，包裹在 <summary> 标签中，包含Goal、Progress、Key Files等结构化内容 5.3 多次压缩的累积效应 压缩次数 信息状态 1-2次 近乎完整，保留具体文件路径和决策 3-5次 早期具体细节开始模糊 5+次 早期信息高度抽象化 10+次 最早工作内容可能完全消失 5.4 缓解信息损失的机制 迭代更新 ：在原摘要基础上追加新信息，非摘要的摘要 Memory flush ：压缩前持久化关键信息至 memory/*.md 文件操作记录累积 ： readFiles 、 modifiedFiles 跨压缩保留 Tool失败保留 ：最多8条tool failures附加到摘要末尾 🔧 无Embedding模型时的完整影响 当未配置任何embedding provider时： /new 触发 session-memory hook ↓ 读取最近15条消息 → 生成slug → 写入memory/2026-03-05-some-topic.md ↓ MemoryIndexManager尝试索引 → embedding provider="none" ↓ 文件无法chunk+embed+存入SQLite → memory_search返回空结果 组件状态表 ： 组件 状态 原因 对话历史 正常 不需要embedding Compaction压缩 正常 用聊天模型生成摘要 Memory文件 存在但未索引 无embedding provider Memory search disabled 无法生成查询向量 结果 ：系统无跨session长期记忆能力 🚀 实际优化建议 7.1 配置本地Embedding模型 核心结论 ：OpenClaw默认无内置embedding模型，需手动配置才能启用长期记忆 模型评测对比 ： 模型 参数量 维度 大小 nDCG@10 Recall@3 耗时 vs基线 embeddinggemma-300M 300M 768 313MB 0.703 0.813 34s 基线 Qwen3-Embedding-0.6B 600M 1024 610MB 0.804 0.867 118s +14.4% Qwen3-Embedding-4B 4B 2560 4.0GB 0.796 0.880 625s +13.2% 推荐模型 ：Qwen3-Embedding-0.6B（610MB），性价比最优，推理速度是4B模型的5倍 7.2 合理使用Session 推荐模式： 一个任务主题→一个session→完成后/new ，通过session-memory hook保存摘要，新session通过memory_search回忆历史 7.3 搜索时fallback机制改进 建议在 src/memory/manager.ts 中为 embedQueryWithTimeout() 添加try/catch，使embedding失败时退化为纯BM25搜索 🔄 Qwen3-Embedding-0.6B配置步骤 下载模型 &ldquo;`bash ModelScope（国内） curl -L -o ~/.openclaw/models/Qwen3-Embedding-0.6B-Q8_0.gguf &rdquo; https://www.modelscope.cn/models/Qwen/Qwen3-Embedding-0.6B-GGUF/resolve/master/Qwen3-Embedding-0.6B-Q8_0.gguf&rdquo; 或HuggingFace curl -L -o ~/.openclaw/models/Qwen3-Embedding-0.6B-Q8_0.gguf &ldquo; https://huggingface.co/Qwen/Qwen3-Embedding-0.6B-GGUF/resolve/main/Qwen3-Embedding-0.6B-Q8_0.gguf&rdquo; ```bash openclaw config set agents.defaults.memorySearch.local.modelPath \ "~/.openclaw/models/Qwen3-Embedding-0.6B-Q8_0.gguf" ```3. **验证配置** ```bash openclaw memory status --deep 需确认：Provider=local、Model路径正确、Embeddings=ready、Vector=ready 4. 重建索引 rm ~/.openclaw/memory/main.sqlite openclaw memory index --force 🔬 Hybrid Search原理与调优 9.1 混合检索机制 Vector搜索 ：Embedding→余弦相似度，理解语义关联 BM25搜索 ：词频+逆文档频率，精确匹配关键词 合并策略 ：vectorWeight=0.7，textWeight=0.3，按最终分数排序 9.2 关键参数调优 参数 默认值 建议值 说明 maxResults 6 6-8 控制返回条数，避免上下文悬崖 minScore 0.35 0.35-0.45 Qwen3模型推荐0.35-0.45 hybrid.vectorWeight 0.7 0.7 符合行业最佳实践 hybrid.candidateMultiplier 4 4-6 候选池倍数，平衡召回与延迟 📌 补充细节 上下文悬崖现象 ：研究发现注入超过2,500 tokens后，检索内容越多反而降低LLM回答质量 懒加载机制 ：memory索引仅在首次搜索、CLI命令或会话启动时初始化，避免gateway启动延迟 摘要迭代更新规则 ：第二次及以后压缩使用 UPDATE_SUMMARIZATION_PROMPT ，要求&rdquo;保留所有现有信息+添加新内容+更新状态&rdquo; Qwen3-Embedding优势 ：支持instruction prefix，加前缀后nDCG提升14.4%，目前代码未实现需等待后续版本