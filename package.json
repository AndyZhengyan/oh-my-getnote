{
  "name": "oh-my-getnote",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "cd web && npx eslint .",
    "build": "cd web && npx next build"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.27.2"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
